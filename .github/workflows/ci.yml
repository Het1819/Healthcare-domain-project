name: FastAPI CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

# This environment variable fixes the Node.js 20 warning
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Build and Run Docker Container
        run: |
          # Changed to modern 'docker compose' (no hyphen)
          docker compose up -d --build
          
          echo "Waiting for the server to start..."
          sleep 15

      - name: Check Container Logs (Debugging)
        # Changed to modern 'docker compose'
        run: docker compose logs

      - name: Test /health endpoint
        run: |
          STATUS_CODE=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/health)
          
          if [ "$STATUS_CODE" -eq 200 ]; then
            echo "✅ Health check passed! Received status $STATUS_CODE"
          else
            echo "❌ Health check failed! Received status $STATUS_CODE"
            exit 1
          fi

      - name: Stop and Remove Container
        if: always()
        # Changed to modern 'docker compose'
        run: docker compose down